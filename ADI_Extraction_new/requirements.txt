# ADI Extraction - Requirements
# Azure Document Intelligence PDF Processing Pipeline
# Only packages actually used in the current codebase

# LangChain Framework (used in pdfextraction.py)
langchain-core>=0.2.0
langchain-community>=0.2.0

# Azure Document Intelligence SDK (required for AzureAIDocumentIntelligenceLoader)
azure-ai-documentintelligence>=1.0.0
azure-identity>=1.15.0
azure-core>=1.29.0

# Data Models and Validation (used in core/models.py)
pydantic>=2.0.0

# Configuration Files (used in core/config_loader.py)
PyYAML>=6.0.1

# Environment Variables (used in core/config_loader.py)
python-dotenv>=1.0.0

# Progress tracking (commonly used in CLI tools)
tqdm>=4.65.0

# PDF Image Extraction (used in pdfextraction.py for page images)
PyMuPDF>=1.23.0
Pillow>=10.0.0